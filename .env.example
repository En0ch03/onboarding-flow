# Optional. The app finds the development server on its own, so nothing here is
# required to run the project locally.
#
# Set this only to point the app at a different API - a staging or production
# host. When it is set it always wins.
#
# EXPO_PUBLIC_API_URL=https://api.example.com/api/v1
